# This project representing a categories tree with CRUD functionality. It`s also implemented exponential DB retry logic (EF feature). It has logging to a text file and log saving and deleting events.
